apiVersion: v1
kind: PersistentVolume
metadata:
  name: postgres-pv
  namespace: icgroup
  labels:
    env: prod
    app: pv
spec:
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeClaimPolicy: delete
  reclaimPolicy: Retain
  hostPath:
    path: /data-postgres